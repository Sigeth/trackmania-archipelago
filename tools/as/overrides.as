// Hand-written fixups for what gen_stubs.py cannot model correctly.
// Loaded AFTER generated/openplanet.stub.as, so anything here that the generator
// also emitted would collide -- keep the generator's skip lists in sync.
//
// Add an entry here only with a one-line reason.

// (currently empty -- the generated surface compiles the plugin as-is)
