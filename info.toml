[meta]
name        = "Archipelago"
author      = "Sigeth"
version     = "0.1.0"
category    = "Multiworld"
description = "Integrates Trackmania Turbo with the Archipelago.gg multiworld randomizer."

# NOTE: Openplanet compiles EVERY *.as anywhere under the deployed plugin folder
# into one module — there is no source include/exclude list in info.toml. Keep
# non-plugin AngelScript (the tools/as/ stub, tests, overrides, and its CMake
# _deps tree) out of what gets deployed: the dev install links only info.toml +
# src/ (see CLAUDE.md "Dev install"), which is also exactly what the .op ships.
